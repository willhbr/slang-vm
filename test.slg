(module Main)

(defn print-all-types [types]
  (-> types
    Vector.head
    Kernel.type
    IO.puts)
  (if (Vector.head types)
    (recur (Vector.tail types)))
    nil)

(print-all-types [1 "hello" [1 2 3] (List.new 1 2 3)])
